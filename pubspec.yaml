name: automata
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies: 
  meta: ^1.7.0
  collection: ^1.15.0

dev_dependencies:
  test: ^1.20.1
  flutter_lints: ^1.0.0
  mocktail: ^0.2.0